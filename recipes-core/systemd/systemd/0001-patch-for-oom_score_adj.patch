From be28f89536a64fec1bb3876b1535c4a1cfacc95e Mon Sep 17 00:00:00 2001
From: Sangdol Kim <doliyu@marusys.com>
Date: Mon, 3 Dec 2012 18:59:57 +0900
Subject: [PATCH 1/2] patch for oom_score_adj

---
 src/core/execute.c |    5 +++--
 1 files changed, 3 insertions(+), 2 deletions(-)

diff --git a/src/core/execute.c b/src/core/execute.c
index e502c24..4a088bd 100644
--- a/src/core/execute.c
+++ b/src/core/execute.c
@@ -1185,13 +1185,14 @@ int exec_spawn(ExecCommand *command,
                         }
                 }
 
-                if (context->oom_score_adjust_set) {
+                if ( 0 ){ //context->oom_score_adjust_set) {
                         char t[16];
 
                         snprintf(t, sizeof(t), "%i", context->oom_score_adjust);
                         char_array_0(t);
 
-                        if (write_one_line_file("/proc/self/oom_score_adj", t) < 0) {
+                        //if (write_one_line_file("/proc/self/oom_score_adj", t) < 0) {
+                        if (write_one_line_file("/proc/self/oom_adj", t) < 0) {
                                 err = -errno;
                                 r = EXIT_OOM_ADJUST;
                                 goto fail_child;
-- 
1.7.2.3

